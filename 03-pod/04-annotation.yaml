apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  annotations:
    description: "This pod runs the nginx web server for the development environment"
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80