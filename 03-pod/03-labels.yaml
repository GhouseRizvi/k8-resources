apiVersion: v1
kind: Pod
metadata:
  name: labels
  labels:
    app: myapp
    env: production
    version: v1
    author: ghouse-rizvi
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80