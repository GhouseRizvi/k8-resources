In Kubenetes we've Cluster level resources / namespace level resources

POD:
kubectl api-resources | pods

in pod all the container share the smae ip address.

image pull policy exhibit the behaviour that it should not use the local image instead it should pull from the repository.

