apiVersion: v1
kind: Namespace
metadata:
  name: nginx
 # namespace: roboshop

spec:
  containers:
    - name: nginx
      imagePullPolicy: Always
      image: nginx:1.14.2
      ports:
        - containerPort: 80