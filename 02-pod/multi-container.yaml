apiVersion: v1
kind: Pod
metadata:
  name: multiple
  namespace: roboshop
spec:
  containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
        - containerPort: 80
    - name: almalinux
      image: almalinux:8
      command: ["sleep", "3600"]