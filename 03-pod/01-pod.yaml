apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
    - name: nginx-container
      #image: nginx:iii this will cause the pod to fail because the image does not exist imagepullbackoff error
      image: nginx:latest
      ports:
        - containerPort: 80